$("#currentDay").text(moment().format("[Today is] dddd, MMM Do, YYYY"));

var rightNow = moment();

//add appropriate class for each children of the container to denote past, present, or future

//localstorage for text area input to save the events. events must persist across reloads